---
status: design-handoff
owner: BABAI Architecture
last-reviewed: 2026-09-21
sources:
  - nekurama.chatgpt.md
  - nekurama.raw.chat.json
  - docs/products/babai/architecture.md
  - docs/products/babai/architecture-boundaries.md
  - docs/products/babai/architecture-lld.md
  - docs/products/babai/domain-model.md
  - docs/products/babai/product-definition.md
  - docs/products/babai/experience-and-channels.md
  - docs/products/babai/tier-feasibility-matrix.md
  - docs/products/babai/validation.md
---

# BABAI flow architecture matrix

## Purpose and status

This is the durable flow-by-flow architecture handoff for BABAI. It turns the
founder-flow discussion into explicit ownership, contracts, reliability and
pilot gates without claiming that a backend implementation or provider
approval already exists.

The request named thirteen flows but asked for fourteen families. The
fourteenth family is made explicit here as **identity / tenant / channel /
entitlement**, because every other flow depends on trusted context resolution
and tier authorization. Billing-account and subscription transitions are part
of that family and the onboarding family; they are not allowed to become an
implicit shortcut around domain authorization.

Architecture status uses:

- **Specified** — ownership and contract direction are documented below and
  in the linked architecture/domain documents.
- **POC/pilot gate** — required to validate the first paid pilot or a
  controlled-beta expansion.
- **Later** — deliberately not a first-pilot dependency.
- **Implementation/demo** — this branch contains architecture documentation,
  not a running BABAI backend. Unless stated otherwise, implementation is
  **not started** and no executable product demo is claimed.

  Canonical command/event naming is maintained in
  [`contracts.ts`](contracts.ts) and [`events-catalog.md`](events-catalog.md):

  - customer `RequestCancellation` creates a reviewable
    `OrderCancellationRequested` event;
  - authorized staff/owner `CancelOrder` creates `OrderCancelled` after policy
    and transition validation;
  - recovery `AuthorizeReplay` creates `ReplayAuthorized`; the past-tense event
    is never dispatched as a command;
  - provider callbacks, manual confirmation, refund, notification, voucher,
    promotion, exception and DLQ commands must emit the corresponding
    versioned event and audit record.

  Raw-founder citations use both exact source locations:

`Raw T<number> (<mapping/message UUID>); nekurama.raw.chat.json:L<line>; nekurama.chatgpt.md:L<line-range>`.

The raw JSON line is the mapping object in the committed current revision.
The readable Markdown line range is a companion locator, not a substitute for
the UUID. Assistant turns are retained as decision history; founder-authored
turns remain primary evidence where the source distinguishes them. `01 /
THE MOCK FLOW` is intentionally excluded.

## Cross-cutting contract used by every family

```text
Inbound webhook / web action / worker command
  -> authenticate and verify provider/request context
  -> resolve identity, billing account, tenant, branch, channel and actor
  -> load entitlement and policy version
  -> enforce capability, quota and provider admission
  -> validate the owning aggregate transition
  -> PostgreSQL transaction:
       authoritative state transition
       outbox record
       required audit/provenance record
  -> bounded command result
  -> managed queue / worker side effects
  -> idempotent consumers, notifications, reconciliation and metrics
```

Every command/event should carry, subject to classification:

`eventId`, `eventType`, `schemaVersion`, `occurredAt`, `recordedAt`,
`producer`, `tenantId`, `branchId`, `channelId` where applicable, `actorRef`,
`aggregateType`, `aggregateId`, `correlationId`, `causationId`, `flowId`,
`idempotencyKey`, `classification`, and a typed payload or payload reference.

The baseline is at-least-once delivery with transactional outbox, inbox/
deduplication, bounded retries, quarantine/DLQ, authorized replay and
provider reconciliation. A failure must never become success-shaped domain
state. Payment, order and fulfillment are separate state machines. Human
takeover pauses conversational automation only; it does not pause, rewrite or
silently bypass order, payment, fulfillment, authorization, outbox or
reconciliation processing.

## Expanded journey contract matrix

The fourteen-family table below defines capability ownership. This companion
matrix is the implementation-facing contract for the expanded journeys named
in the current scope. Each row identifies the authoritative state, typed API
surface, event vocabulary, permission gate, deduplication key and recovery
behavior. A button, sample WhatsApp transcript or dashboard card in the
founder discussion is a **mock interaction** until this contract is backed by
an authenticated command, an aggregate transition, an outbox record and an
auditable result.

| Journey | State and owner | API / commands | Events | Permission / tenant rule | Idempotency / deduplication | Recovery / replay | Boundary and current status | Source anchors |
|---|---|---|---|---|---|---|---|---|
| **Onboarding invite and status** | `OnboardingCase`: `DRAFT → INVITED → OWNER_CONNECTED → MENU_PENDING → PLAN_PENDING → PAYMENT_PENDING → ACTIVE`, with `BLOCKED`, `EXPIRED`, `CANCELLED`. Admin creates/invites; owner accepts; BillingAccount/Entitlement activates. | `POST /onboarding-cases`; `AcceptInvite`; `GetOnboardingStatus`; `ResendInvite`; `SelectPlan`; `ConfirmActivation`. | `OnboardingStarted`, `InviteSent`, `OwnerConnected`, `OnboardingBlocked`, `PlanSelected`, `SubscriptionPaid`, `TenantActivated`, `InviteExpired`. | Admin can initiate only within platform scope; invite acceptance is bound to verified recipient and intended tenant; status is visible only to authorized admin/owner scope. Payment pending never grants paid capabilities. | `onboardingCaseId`, invite token hash, resend sequence and subscription provider event ID. Repeated status reads are safe; resend is deduplicated by case and active invite version. | Expired invite creates a new version; provider/send failure retries; invalid recipient or scope quarantines. Replay re-emits status from authoritative case state, not from an old WhatsApp transcript. | **Production contract / P0 pilot.** Sample invitation wording and status cards are mock-only; no backend onboarding implementation exists. | Founder Raw T5 (`bbb21d44-faca-4e4b-a6e2-9f65f591a4bd`), `nekurama.raw.chat.json:L64306`, `nekurama.chatgpt.md:L1140-L1474`; [`product-definition.md`](product-definition.md), [`architecture-lld.md`](architecture-lld.md). |
| **Menu extraction, correction and publish** | `MenuRevision`: `RECEIVED → EXTRACTING → NEEDS_CORRECTION → VERIFIED → PUBLISHED`, with `REJECTED`/`QUARANTINED`. Catalog/Menu owns revision; AI/extractor only drafts; owner/staff verifies and publishes. | `UploadMenu`; `ExtractMenu`; `GetExtraction`; `PatchMenuDraft`; `SubmitCorrection`; `VerifyMenu`; `PublishMenuRevision`; `GetPublishedMenu`. | `MenuUploaded`, `MenuExtractionStarted`, `MenuDraftCreated`, `MenuCorrectionRequested`, `MenuVerified`, `MenuPublished`, `ExtractionQuarantined`. | Upload/edit/publish requires tenant/branch menu scope; customer reads published revision only. AI cannot publish, alter price or availability without a typed authorized command. | File checksum + object version + extractor/model version; `menuRevisionId`; correction command key; publish uses optimistic revision version. | Low-confidence extraction goes to correction; transient storage/model errors retry; poison files quarantine. Replay re-extracts the immutable source into a new draft and never overwrites a published revision. | **Production contract / P0 pilot.** “PDF/image becomes three menu items” is a mock example; extraction provider and quality are POC dependencies. | Founder flow `nekurama.chatgpt.md:L1321-L1377` and `L1883-L1913`; Raw T5 (`bbb21d44-faca-4e4b-a6e2-9f65f591a4bd`), raw `L64306`; [`domain-model.md`](domain-model.md), [`architecture-lld.md`](architecture-lld.md). |
| **Staff roles and scoped actions** | `Membership`/`RoleAssignment`: `INVITED → ACTIVE → SUSPENDED/REMOVED`; role policy is separate from order/menu/payment state. Owner manages membership; staff executes permitted work. | `InviteStaff`; `AcceptStaffInvite`; `AssignRole`; `GrantBranchScope`; `SuspendStaff`; `ListPermittedActions`; `AuthorizeCommand`. | `StaffInvited`, `MembershipActivated`, `RoleAssigned`, `ScopeChanged`, `StaffSuspended`, `AuthorizationDenied`. | `Identity + Membership + Role + Permission + Scope + Resource + Policy → Decision`; default deny; branch and tenant scope are mandatory. Staff cannot elevate entitlement, mark payment paid without permission or cross tenant data. | Invitation ID/version; membership mutation key; authorization decision cache keyed by policy version; repeated action button returns current aggregate state. | Stale role version forces refresh; unauthorized action is rejected/audited; lost device or provider outage suspends/requeues work. Replay is a reviewed command with a new authorization decision, not a replayed UI click. | **Production contract / P0 pilot for owner/staff operations.** Role names and WhatsApp button examples are mock UX; RBAC implementation is not present. | Founder flow `nekurama.chatgpt.md:L1678-L1740`; Raw T209 (`bbb21545-4598-4ecd-a1fa-af977810bd5b`), raw `L60919`; Raw T216 (`bbb216f2-0ccd-4871-a670-e02f94100750`), raw `L61541`; [`domain-model.md`](domain-model.md), [`architecture-boundaries.md`](architecture-boundaries.md). |
| **Customer → menu → cart** | `Conversation` owns interaction context; `TenantCustomer` owns tenant relationship/consent; `Cart` owns mutable intent. `MenuRevision` is read-only input; no order exists until submit. | `ResolveInbound`; `GetMenu`; `SelectBranch`; `CreateCart`; `AddCartItem`; `ChangeQuantity`; `RemoveCartItem`; `ValidateCart`; `GetCart`. | `ConversationStarted`, `BranchSelected`, `MenuViewed`, `CartCreated`, `CartChanged`, `CartInvalidated`, `CartReadyForCheckout`. | Receiving channel resolves tenant; shared number requires explicit branch when material. Customer can access only own tenant relationship and cart. No silent identity merge. | Provider message ID + channel scope; cart version; `(tenantCustomerId, branchId, activeCartId)` uniqueness; repeated add/remove with command key is safe. | Menu revision or availability change returns explicit correction; unknown/ambiguous customer asks for confirmation; unsupported intent routes to takeover. Replay rebuilds conversation/cart projection without submitting an order. | **Production contract / P0 pilot.** Sample menu buttons and cart transcript are mock-only; restaurant-owned WhatsApp ingress is the production target. | Founder Raw T34 (`bbb21cdc-296c-4806-af69-c1b7f40c4cf8`), raw `L63949`; Raw T99 (`bbb21fb5-8f72-4a7f-b42b-333101d4a900`), raw `L65592`; chat `L1478-L1520`; [`domain-model.md`](domain-model.md), [`experience-and-channels.md`](experience-and-channels.md). |
| **Payment / order separation** | `Order`: `DRAFT → SUBMITTED → ACCEPTED/REJECTED`; `Payment`: `PENDING → AUTHORIZED/PAID/FAILED/UNKNOWN`, with refund states. Neither owns the other’s transition; Order references Payment and Payment references Order. | `SubmitOrder`; `CreatePaymentIntent`; `RecordPaymentCallback`; `RecordManualConfirmation`; `AcceptOrder`; `RejectOrder`; `RequestRefund`; `ReconcilePayment`. | `OrderSubmitted`, `PaymentInitiated`, `PaymentPaid/Failed/Unknown`, `OrderAccepted/Rejected`, `RefundRequested/Completed`, `PaymentReconciled`. | Customer/merchant/provider scope is checked independently. LITE payment is unavailable; BASE/PRO require entitlement and configured merchant settlement. A payment callback cannot accept an order. | Order submission key, payment intent key, provider event ID + merchant/channel scope, unique order/payment attempt, refund key. | Unknown payment reconciles before any compensation; static QR/UPI/cash remains `MANUAL_CONFIRMATION_REQUIRED`; provider timeout retries/reconciles; invalid callback quarantines. Replay is reconciliation or an authorized refund command, never “mark paid”. | **Production contract / P0 pilot** for direct merchant settlement plus manual confirmation; provider/KYC/refund validation remains open. | Founder Raw T68 (`bbb21613-1461-4278-a34f-d4c055c03c84`), raw `L61167`; Raw T72 (`bbb21c29-06e2-44d7-bd05-c2f9c28c3f4f`), raw `L63619`; chat `L2508-L2559`; [`architecture-lld.md`](architecture-lld.md), [`domain-model.md`](domain-model.md). |
| **Pickup / delivery tier gates** | `Fulfillment`: pickup `READY_FOR_PICKUP → COMPLETED`; delivery `REQUESTED → QUOTED → CONFIRMED → ASSIGNED → IN_TRANSIT → DELIVERED`, with failure/cancel states. Fulfillment is independent from Order and Payment. | `ChooseFulfillmentMode`; `GetQuote`; `ConfirmAddress`; `ConfirmDelivery`; `BookDelivery`; `UpdatePickupReady`; `ConfirmCollection`; `ReconcileTracking`. | `FulfillmentSelected`, `QuoteReceived`, `DeliveryConfirmed`, `DeliveryBooked`, `DriverAssigned`, `PickupReady`, `Collected`, `Delivered`, `DeliveryFailed`. | Pickup is the P0 baseline. Delivery requires BASE/PRO entitlement, branch configuration, valid provider/geography and explicit customer confirmation. LITE must return unavailable/not-applicable, never silently downgrade. | Fulfillment ID; quote expiry/version; provider booking key; provider event ID + tenant/branch/fulfillment scope. | Delivery unavailable falls back to explicit pickup choice; expired quote requires re-quote; ambiguous booking reconciles; provider failures retry/DLQ. Replay re-queries provider status. | **Pickup production contract / P0; delivery provider P1 gated beta.** Porter/Rapido/Uber names, sample prices and tracking cards are not approvals or demos. | Founder Raw T19 (`bbb216eb-b95f-4255-b182-0b9e8dc39288`), raw `L61497`; Raw T21 (`bbb21b01-c1d5-42f9-9e1f-702bb346453c`), raw `L63289`; chat `L2285-L2422`, `L2559-L2626`; [`product-definition.md`](product-definition.md), [`tier-feasibility-matrix.md`](tier-feasibility-matrix.md). |
| **Order desk states** | `Order` operational desk: `NEW/SUBMITTED → ACCEPTED → PREPARING → READY → COMPLETED`, with `REJECTED`, `CANCELLED`, `CORRECTION_PENDING`, `PAYMENT_REVIEW` and `FULFILLMENT_EXCEPTION`. Order module owns transitions; staff/owner operate through scoped commands. | `ListOpenOrders`; `GetOrder`; `AcceptOrder`; `RejectOrder`; `StartPreparation`; `MarkReady`; `CompleteOrder`; `RequestCorrection`; `CancelOrder`; `AcknowledgeException`. | `OrderReceived`, `OrderAccepted`, `OrderRejected`, `OrderPreparing`, `OrderReady`, `OrderCompleted`, `OrderCorrectionRequested`, `OrderCancelled`, `OrderExceptionRaised`. | Staff action requires branch/order scope and current transition version; owner/admin may have broader tenant scope. Customer cannot force operational state. | Transition key `(orderId, expectedVersion, commandKey)`; duplicate webhook/button returns current state; provider event IDs are inbox-deduplicated. | Stale action returns current state; staff outage queues notification/task; correction may trigger payment/refund review. Replay is a reviewed state command. | WhatsApp action messages and web order desk are client adapters; provider transport does not own state. **P0 production contract/pilot.** Sample `#1042` cards, buttons and push notifications are mock-only; test duplicate/stale/rejected/correction/payment-review/pickup paths and observe transition latency, stale-command rate, missed/duplicate actions and unresolved exceptions. | Founder flow `nekurama.chatgpt.md:L1520-L1678`; Raw T209 (`bbb21545-4598-4ecd-a1fa-af977810bd5b`), raw `L60919`; Raw T218 (`bbb21ee2-2265-4c8a-ba89-82249c36e181`), raw `L65262`; [`domain-model.md`](domain-model.md), [`validation.md`](validation.md). |
| **Human takeover** | `Conversation`: `AUTOMATED → HUMAN_REQUESTED → HUMAN_ACTIVE → HUMAN_RELEASED → AUTOMATED`; assignment/lease owns human work. Order, Payment and Fulfillment continue independently. | `RequestHuman`; `AssignConversation`; `PauseAutomation`; `SendHumanMessage`; `ReleaseToAutomation`; `ResumeAutomation`. | `HumanRequested`, `ConversationAssigned`, `AutomationPaused`, `HumanMessageSent`, `AutomationReleased`, `TakeoverExpired`. | Assigned/authorized owner or staff can act only within tenant/branch/channel scope. AI cannot release takeover or commit controlled state without normal authorization. | Takeover key `(conversationId, assignmentVersion)`; provider message ID and outbound send key suppress duplicates; worker checks active lease before sending. | Automation workers suppress/requeue sends; unavailable staff creates an explicit support task; provider failures retry/DLQ. Replay rehydrates conversation history and assignment, not stale outbound side effects. | Meta/WhatsApp coexistence and web inbox are provider adapters. **P0 production safety contract/pilot.** Chat handoff copy and multi-agent examples are mock-only; observe suppression violations, assignment latency, human response time and abandonment. | Raw T94 (`53e3d28d-2172-4b99-9d5d-cefc16513fa9`), raw `L21496`; Raw T96 (`43953802-8fdc-4ee4-a329-782796871b42`), raw `L16533`; chat `L522-L557`; [`experience-and-channels.md`](experience-and-channels.md), [`domain-model.md`](domain-model.md). |
| **Promotions / combos: limits and expiry** | `Promotion`/`Combo` lifecycle: `DRAFT → ACTIVE → EXPIRED/PAUSED`; evaluation is bounded and deterministic. Price owns combo/bundle construction; Promotion owns benefits; Order snapshots result. | `CreatePromotion`; `CreateCombo`; `ActivatePromotion`; `EvaluateCart`; `ReserveQuota`; `ExpirePromotion`; `PausePromotion`; `ApplyBenefit`. | `PromotionCreated`, `PromotionActivated`, `PromotionExpired`, `PromotionPaused`, `CommercialEvaluated`, `BenefitApplied/Rejected`, `QuotaRejected`. | Tenant/branch owner scope; LITE unavailable; BASE bounded (provisional max 3 requests/day and short activation); PRO advanced only with policy/quota. Customer eligibility is not client-selected. | Evaluation key `(tenant, cart, ruleVersion, capsuleHash)`; atomic quota reservation; expiry version prevents late activation; repeated evaluation is a no-op for same facts/version. | Expired/ineligible rules are explicit no-benefit outcomes; malformed rules quarantine; replay evaluates a new draft/version and never rewrites an order. | Core calculation has no external provider; notification/campaign adapters are downstream and consent-gated. **P1 production contract/gated beta.** Food-themed tier names, offer examples and limits remain provisional; test expiry/timezone/quota/stacking/combo/tax and contribution signals. | Founder Raw T21 (`bbb21b01-c1d5-42f9-9e1f-702bb346453c`), raw `L63289`; chat `L2439-L2673`; [`domain-model.md`](domain-model.md), [`tier-feasibility-matrix.md`](tier-feasibility-matrix.md). |
| **Notifications, reorder, refund and recovery** | Separate `NotificationIntent`, `TenantCustomer`/`ReorderIntent`, `PaymentRefund` and `RecoveryTask` states. Notification delivery never becomes order/payment truth; reorder creates a new cart/order; refund is owned by Payment. | `CreateNotification`; `ResolveConsent`; `SendTemplate`; `StartReorder`; `ConfirmRepeatCart`; `RequestRefund`; `ReconcileRefund`; `CreateRecoveryTask`; `ResolveDLQ`. | `NotificationRequested/Sent/Failed`, `OptedOut`, `RepeatOrderSuggested/Completed`, `RefundRequested/Completed`, `RecoveryTaskCreated/Resolved`, `Reconciled`. | Transactional/support consent is distinct from marketing opt-in; reorder history is tenant-scoped; refund requires payment/order scope and authorized actor. LITE/BASE/PRO gates are enforced server-side. | Notification key `(tenant, subject, category, event, version)`; reorder key `(tenantCustomer, sourceOrder)`; refund/provider event ID; recovery run ID; duplicate resend/refund is rejected. | Service-window/template failure retries or quarantines; marketing is suppressed without opt-in; unavailable reorder items create a fresh correction path; refund unknown reconciles; DLQ actions require approval. Replay re-evaluates consent/status/version and never blindly resends or refunds. | Meta/WhatsApp, email/SMS/push, payment and delivery adapters remain replaceable. **Transactional/refund recovery P0; reorder P1; marketing/win-back later/PRO-gated.** Prove status survives opt-out, repeat order is new, refund links to payment and recovery has no duplicate side effects; measure delivery failure, opt-out, repeat conversion, refund latency, DLQ age and recovery cost. | Raw T63 (`2f7e2590-8665-4398-b7d6-334e994d4ab2`), raw `L11878`; Raw T290 (`4702681b-d611-4408-af5f-9001d04b6cfa`), raw `L18554`; Raw T216 (`bbb216f2-0ccd-4871-a670-e02f94100750`), raw `L61541`; chat `L7708-L7902`, `L587-L611`; [`architecture-lld.md`](architecture-lld.md), [`validation.md`](validation.md). |
| **Admin funnel and analytics** | `OnboardingCase` and domain aggregates remain authoritative; read models own `ONBOARDING_FUNNEL`, `ORDER_DESK`, `COST_LEDGER`, `PILOT_METRICS` projections. Analytics is eventually consistent and never authorizes a transition. | `GetOnboardingFunnel`; `ListOperationalExceptions`; `GetOrderMetrics`; `GetTierUsage`; `GetCostLedger`; `ExportTenantData`; `ReviewAudit`; `RebuildProjection`; `RecordPilotMetric`. | `ProjectionUpdated`, `ProjectionLagged`, `ProjectionRebuildStarted/Completed`, `PilotMetricRecorded`, `ExportReady`. | Platform admin sees platform funnel; owner sees own tenant/branch; staff sees operational subset. Cross-tenant analytics and exports require explicit platform policy and audit. | Projection consumer key `(projectionName, eventId, schemaVersion)`; export job ID/checksum; repeated reads are safe and rebuilds do not mutate domain state. | Projection lag/rebuild replays retained events; malformed event quarantines without altering authoritative state; query timeout returns async export/retry. Deletion/retention must propagate to projections. | Web/BFF and bounded admin WhatsApp are clients; telemetry/read-store/object-storage providers are adapters. **P0 production contract/pilot** for onboarding/order/error/support funnel; advanced BI/warehouse later. Reconcile counts to authoritative state within stated lag, show stale/error state and block cross-tenant reads; measure lag, latency, export failures, completeness and audit reads. | Founder flow `nekurama.chatgpt.md:L1774-L1845`; Raw T100 (`39e2f9bb-f490-461b-a7c0-ddc8baf13701`), raw `L14361`; [`architecture-lld.md`](architecture-lld.md), [`validation.md`](validation.md), [`experience-and-channels.md`](experience-and-channels.md). |

## Mock-only versus production boundary

| Surface or claim | Mock-only / illustrative status | Production or pilot boundary |
|---|---|---|
| Founder transcript buttons, sample order numbers, sample prices, plan names and WhatsApp copy | Illustrative UX and discovery evidence. They do not define an API, provider approval, price, entitlement or successful state transition. | Replace with authenticated typed commands, policy decisions, aggregate transitions, outbox/audit records and provider-backed delivery evidence. |
| Static site/dashboard preview and sample analytics cards | Presentation/demo artifact only; it has no durable tenant, order, payment, consent or staff state. | A production dashboard reads authorized projections and displays freshness/error state; it cannot become a second source of truth. |
| Menu PDF/image extraction, AI replies, automated staff actions | POC hypotheses. AI/extraction may draft or recommend; examples are not proof of accuracy or safe side effects. | Owner/staff review, deterministic validation, explicit permissions, correction state, versioned provenance and replayable source artefacts are required. |
| Meta/WhatsApp coexistence, Direct Send/templates, payment gateway, delivery providers and email/SMS | External/provider validation dependencies. Provider names in founder discussion are candidates, not approvals or commitments. | Use replaceable adapters, verified callbacks, provider event IDs, throttling, reconciliation, exit behavior and explicit unavailable outcomes. |
| Tiers, quotas, promotions, delivery and marketing | Provisional entitlement experiments, not public pricing or unlimited capability promises. | Enforce LITE/BASE/PRO server-side at gateway, worker and owning module; consume quotas atomically and audit denials/overrides. |

The production baseline is therefore the architecture/domain contract, not the
appearance of a mock conversation: modular-monolith modules, PostgreSQL
authoritative state, transactional outbox/inbox, provider adapters, direct
merchant settlement, pickup-first fulfillment, human takeover without
cross-state corruption, and explicit pilot observability. [`architecture.md`](architecture.md);
[`architecture-lld.md`](architecture-lld.md); [`domain-model.md`](domain-model.md);
[`product-definition.md`](product-definition.md).

## Fourteen-family matrix

| # / family | Actor and authoritative state machine ownership | Commands, events, APIs and adapters | Idempotency / deduplication | Authorization and tenant boundary | Audit / provenance | Alternate, error, retry, DLQ and replay path | Provider boundary | POC / pilot / later priority | Acceptance and observability gates | Architecture / implementation / demo status | Evidence anchors |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **1. Identity / tenant / channel / entitlement** | Actors: platform admin, owner, staff, customer, Meta/provider. `Identity`, `Membership`, `BillingAccount`, `Tenant`, `Branch`, `Channel` and entitlement projections own their states. Channel lifecycle: `DISCOVERED → AUTHORIZING → CONNECTED → VERIFYING → CONFIGURING → READY`, with `SUSPENDED` / `DISCONNECTED` / `FAILED`. A shared number resolves tenant first and requires explicit branch selection when branch is not trusted from the channel. | `ResolveContext`, `AuthorizeActor`, `ConnectChannel`, `VerifyChannel`, `SelectBranch`, `ChangeEntitlement`; `ContextResolved`, `ChannelConnected`, `ChannelSuspended`, `EntitlementChanged`. Web onboarding and WhatsApp admin entry points use the same typed contracts. Meta Embedded Signup/Cloud API is an adapter, not the domain identity. | Provider webhook ID + receiving channel scope; connection attempt ID; command idempotency key; unique `(provider, providerAccountRef, phoneRef)` and scoped membership/entitlement keys. Repeated branch selection is a no-op only when the selected branch is still valid. | Resolve from trusted receiving channel/provider mapping, then enforce `BillingAccount → Tenant → Branch → Channel` scope and actor membership/role/capability/policy. Never trust client-supplied tenant, branch, plan or event type. Customer identity may be global; relationship and consent are tenant-scoped. | Record provider asset refs, ownership/authorization evidence, policy version, actor, branch selection, entitlement decision, old/new status and reason. Preserve menu/order/payment references independently of a later channel disconnect. | Invalid signature/scope/policy is non-retryable and quarantined. Provider outage retries with backoff; ambiguous connection state waits for reconciliation. Authorized replay re-runs a versioned context command, never a raw client event. Disconnect leaves domain history and marks channel unavailable. | `ChannelPort` → Meta Cloud API / Tech Provider target; BSP fallback behind the same port. Do not make WABA ID the permanent domain identity. | **P0 pilot gate** for one tenant/branch/channel; multi-branch shared-number and per-branch entitlements are **P1 controlled beta**. | Must prove wrong-tenant and wrong-branch requests are rejected, channel disconnect is explicit, entitlement denial is non-success, and reconnect does not duplicate identity. Measure signature failures, context-resolution latency/errors, scope denials, channel state age, entitlement denials and provider reconciliation drift. | **Specified; implementation not started; no executable demo.** Static web artifacts are not evidence of channel onboarding. | Raw T33 (`ded20a72-7392-4785-8983-b6e7d75eae3b`), `nekurama.raw.chat.json:L72637`, `nekurama.chatgpt.md:L4678-L5053`; Raw T34 (`bbb21cdc-296c-4806-af69-c1b7f40c4cf8`), raw `L63949`, chat `L5121-L5526`; Raw T35 (`97f25c92-127f-4c8c-bde9-c314010cbef7`), raw `L46376`, chat `L5283-L5608`; [`architecture.md`](architecture.md), [`domain-model.md`](domain-model.md), [`architecture-lld.md`](architecture-lld.md). |
| **2. Onboarding** | Actors: admin initiates; owner authorizes and supplies business/menu/subscription information; staff may be invited later. `OnboardingCase` owns `INVITED → OWNER_CONNECTED → MENU_RECEIVED → MENU_VERIFIED → PLAN_SELECTED → PAYMENT_PENDING/CONFIRMED → ACTIVE`, plus `BLOCKED`, `EXPIRED`, `CANCELLED`. Technical setup may complete before payment; activation and entitlement are separate decisions. | `StartOnboarding`, `AcceptInvite`, `SubmitBusinessDetails`, `UploadMenu`, `VerifyMenu`, `SelectPlan`, `ConfirmSubscription`, `ActivateTenant`; events `OnboardingStarted`, `OwnerConnected`, `MenuSubmitted`, `MenuVerified`, `SubscriptionSelected`, `SubscriptionPaid`, `TenantActivated`. WhatsApp and web are alternate clients over the same onboarding API; object storage holds controlled uploads. | Onboarding case ID and invite token; upload checksum/version; plan-selection command key; payment provider event ID if subscription billing is used. Duplicate files create a revision or no-op by checksum, never duplicate menu items silently. | Admin may create an invitation only for permitted billing/tenant scope. Owner acceptance binds to the invited contact after verification. Menu, plan and payment changes are tenant-scoped; platform admin “mark paid” is an audited exception, not a client capability. | Store invite issuer/recipient, consent, uploaded-file checksum/source, extraction model/version, owner corrections, menu revision, selected entitlement, payment evidence and activation decision. Do not store credentials or unrestricted PII in payloads/logs. | Missing/invalid menu goes to `NEEDS_CORRECTION`; extraction retries only transiently; poison files quarantine. Payment pending does not activate a paid capability. Expired invites can be reissued with a new idempotency key. Replay reprocesses a verified file revision under its recorded extractor version. | Meta onboarding adapter, object-storage adapter and subscription/payment adapter are replaceable. Exact Meta coexistence and provider approval remain validation dependencies. | **P0 pilot gate** for founder-led onboarding, reviewed menu and one paid pilot tenant. Automated extraction and multi-branch onboarding are **P1**; self-serve scale is **later**. | 100% of pilot tenants have named operating contact, approved channel, reviewed/published menu and signed paid terms before activation. Measure time-to-ready, menu correction rate, invite expiry, extraction latency/failure, activation funnel and onboarding/support hours. | **Specified; implementation not started; no executable demo.** Founder-flow copy exists in source discussion only. | Onboarding flow: `nekurama.chatgpt.md:L1233-L1474`, raw assistant anchor `2ef7af58-e9b1-4d2d-bfff-d01e98fd1ac5` at `nekurama.raw.chat.json:L11763` (historical flow discussion); Raw T21 (`bbb21b01-c1d5-42f9-9e1f-702bb346453c`), raw `L63289`; [`product-definition.md`](product-definition.md), [`tier-feasibility-matrix.md`](tier-feasibility-matrix.md), [`architecture-lld.md`](architecture-lld.md). |
| **3. Owner / staff operations** | Actors: owner, manager, operational staff, platform admin. `Membership/Role/Permission` owns access; `Order`, `Menu`, `Availability` and `SupportTask` retain their own state. Owner may manage configuration and staff; staff receives only scoped operational actions. Staff corrections are explicit commands, not AI mutations. | `InviteStaff`, `AssignRole`, `GrantScope`, `ListOrders`, `AcceptOrder`, `RejectOrder`, `SetPreparing`, `SetReady`, `CompleteOrder`, `RequestCorrection`, `ApproveCorrection`; events `StaffInvited`, `OrderActionAccepted`, `OrderCorrectionRequested/Applied`. WhatsApp action messages and web dashboard are equivalent command surfaces. | Unique invitation and membership IDs; action idempotency key `(actor, aggregate, transitionVersion, commandKey)`; duplicate button/webhook actions return current state. | `Identity + Membership + Role + Permission + Scope + Resource + Policy → Decision`. Branch scope is mandatory for operational actions; owner group scope may select a branch. Staff cannot elevate tier, alter payment truth or cross tenant boundaries. | Audit actor, role/scope, command, before/after state, reason, customer acknowledgement requirement, policy version and source surface (WhatsApp/web). Order corrections retain commercial/payment consequences and original snapshot. | Stale transition version returns current state and requires refresh, not blind retry. Unauthorized actions are rejected and audited. Provider delivery failures retry; unresolved staff work becomes a support queue/DLQ item. Replay is a reviewed transition command, never replaying a UI click. | WhatsApp channel adapter and web BFF; push/browser notification adapter may be added. Provider transport never owns role or order state. | **P0 pilot gate** for owner/staff order handling and human corrections. Rich staff scheduling and multi-branch role administration are **P1/later**. | Staff can process a test order without duplicate transitions; unauthorized branch action is rejected; correction/refund consequence is visible. Measure action latency, stale-command rate, missed/duplicate actions, takeover frequency, staff handling time and support intervention. | **Specified; implementation not started; no executable demo.** | Raw T209 (`bbb21545-4598-4ecd-a1fa-af977810bd5b`), raw `L60919`; Raw T216 (`bbb216f2-0ccd-4871-a670-e02f94100750`), raw `L61541`; Raw T218 (`bbb21ee2-2265-4c8a-ba89-82249c36e181`), raw `L65262`; chat `L1520-L1740`; [`domain-model.md`](domain-model.md), [`architecture.md`](architecture.md). |
| **4. Customer discovery** | Actor: customer. `Conversation` owns session/context; `TenantCustomer` owns tenant relationship and consent. Discovery is not a BABAI marketplace: the customer normally enters through the restaurant-owned WhatsApp number/link/QR. A shared restaurant number may branch-select before cart creation. | `ResolveInboundChannel`, `ResolveCustomer`, `StartConversation`, `ShowRestaurantInfo`, `SelectBranch`, `StartOrder`; events `ConversationStarted`, `CustomerResolved`, `BranchSelected`. `ChannelWebhookPort` and link/QR attribution adapter provide ingress context. | Provider message ID and `(channelId, providerSenderId)`; conversation turn key; repeated greeting/read events are deduplicated. Customer identity matching is conservative; no silent merge. | Receiving phone/channel resolves tenant; customer may be globally known but all exposed history, consent and operations are `TenantCustomer` scoped. Branch selection must be explicit when material and cannot be inferred from a cross-tenant preference. | Record channel, source link/QR/campaign ref, resolution confidence, consent changes, branch choice and conversation correlation. Do not leak another tenant’s history or global identity details. | Unknown customer starts a new relationship; ambiguous identity asks for confirmation. Provider retries use inbox deduplication. Unsupported intent routes to human takeover. Replay rebuilds a conversation projection, not an order. | Meta/WhatsApp adapter; future channels can implement the same `ChannelPort`. Restaurant owns the visible relationship; BABAI is behind it. | **P0 pilot gate** for direct restaurant entry and pickup ordering. Directory/marketplace discovery is **non-goal/later**. | A customer can enter from a restaurant link, see only that tenant’s approved context, select a branch when needed and reach menu/order without an app. Measure inbound volume, resolution errors, branch-selection abandonment, unsupported intents and channel/provider webhook age. | **Specified; implementation not started; no executable demo.** | Raw T33 (`ded20a72-7392-4785-8983-b6e7d75eae3b`), raw `L72637`; Raw T34 (`bbb21cdc-296c-4806-af69-c1b7f40c4cf8`), raw `L63949`; Raw T99 (`bbb21fb5-8f72-4a7f-b42b-333101d4a900`), raw `L65592`; chat `L4678-L5053`; [`architecture.md`](architecture.md), [`experience-and-channels.md`](experience-and-channels.md). |
| **5. Menu / catalog / availability** | Actors: owner/staff publish; customer reads; AI may extract/draft only. `Menu` owns immutable revisions and publication; `Availability` owns temporary/scheduled sellable availability. No generic warehouse inventory in the pilot. | `UploadMenu`, `ExtractMenu`, `EditMenu`, `PublishMenuRevision`, `SetAvailability`, `GetPublishedMenu`; events `MenuRevisionCreated`, `MenuPublished`, `AvailabilityChanged`. Catalog/menu APIs and object-storage/document-extraction adapters are ports. | File checksum and extraction version; `menuRevisionId`; availability command key and optimistic version. Repeated publish of same revision is a no-op. | Owner/staff with menu scope may edit; customer reads only published tenant/branch menu. AI cannot publish or change price/availability. Tier gates: LITE bounded capture; BASE controls; PRO approved advanced integrations. | Immutable revision, source file checksum, extractor/model/version, reviewer, corrections, publication actor/time, price/availability provenance and tenant/branch. Order stores the menu revision used. | Unsupported/low-confidence extraction becomes human correction; transient extractor/storage failures retry; poison files quarantine. Published revision remains available during provider outage. Replay re-extracts from immutable source but requires review before publication. | Object storage and optional extraction/model adapter; no provider writes authoritative menu state. | **P0 pilot gate** for reviewed/published menu and read path. External availability integration and advanced API-assisted updates are **P1/PRO**; inventory suite is **later/non-goal**. | Publish a reviewed revision, show correct prices/variants, reject unavailable items and preserve historical order menu provenance. Measure extraction correction rate, publish latency, stale-cache rate, availability divergence and menu-read errors. | **Specified; implementation not started; no executable demo.** | chat `L1883-L1913` and `L1321-L1377`; Raw T35 (`97f25c92-127f-4c8c-bde9-c314010cbef7`), raw `L46376`; [`domain-model.md`](domain-model.md), [`product-definition.md`](product-definition.md), [`architecture-lld.md`](architecture-lld.md). |
| **6. Order / cart** | Actors: customer creates cart/order; restaurant accepts/rejects and operates; `Cart` owns mutable intent; `Order` owns committed commercial transaction and lifecycle: `DRAFT → SUBMITTED → ACCEPTED/REJECTED → PREPARING → READY → COMPLETED` with cancellation/exception states. Order acceptance is never inferred from payment. | `AddItem`, `ChangeQuantity`, `ApplyCommercialEvaluation`, `SubmitOrder`, `AcceptOrder`, `RejectOrder`, `StartPreparation`, `MarkReady`, `CompleteOrder`, `CancelOrder`; events `CartChanged`, `OrderSubmitted`, `OrderAccepted/Rejected`, `OrderPreparing`, `OrderReady`, `OrderCompleted`, `OrderCancelled`. | Cart mutation key; order submission idempotency key; unique customer/tenant cart; aggregate version/transition token. Replayed submit returns the same order, not a second order. | Customer acts only on own `TenantCustomer` cart/order; staff acts within branch/order scope; owner/admin scope is policy-bound. Order validates entitlement, availability, commercial snapshot and transition version. | Immutable line-item/variant/modifier, price, discount, tax, total, promotion results, `menuRevisionId`, actor, timestamps, channel, correlation and customer acknowledgement. | Stale cart/menu/availability returns correction path; transient side effects retry after commit; invalid transition rejects explicitly. Poison event goes to DLQ. Replay is an authorized command against recorded version; historical order snapshot is never recalculated. | Ordering is provider-neutral; WhatsApp/web are clients; payment and fulfillment are referenced by ID and separate state machines. | **P0 pilot gate** for pickup order capture, accept/reject and completion. Multi-branch, complex cancellation and advanced ordering are **P1**. | Prove duplicate webhook/button cannot duplicate an order, published menu revision is frozen, staff can accept/reject, and customer receives correct current status. Measure initiated→submitted→accepted→completed conversion, transition errors, queue age, duplicate suppression and order integrity incidents. | **Specified; implementation not started; no executable demo.** | chat `L1478-L1678`; Raw T165 (`bbb21849-7c34-4782-a440-5be8c75acb62`), `nekurama.raw.chat.json:L62223`; Raw T209/T216/T218 above; [`domain-model.md`](domain-model.md), [`architecture-lld.md`](architecture-lld.md). |
| **7. Payment** | Actors: customer, merchant, payment provider, staff for manual confirmation/refund exception. `Payment` owns `PENDING → AUTHORIZED/PAID → FAILED`, and `PAID → REFUND_PENDING → REFUNDED` plus `MANUAL_CONFIRMATION_REQUIRED`, `UNKNOWN`, `CANCELLED`. It is separate from `Order`; direct merchant settlement is the default and BABAI has no wallet/escrow. | `CreatePaymentIntent`, `PresentPaymentOption`, `RecordProviderCallback`, `RecordManualConfirmation`, `MarkFailed`, `RequestRefund`, `ConfirmRefund`, `ReconcilePayment`; events `PaymentInitiated`, `PaymentAuthorized/Paid`, `PaymentFailed`, `ManualConfirmationRequested`, `RefundRequested/Completed`, `PaymentReconciled`. `PaymentPort` → UPI/gateway adapter; static QR/UPI ID/manual path is explicit. | Provider event ID + merchant/channel scope; payment intent/idempotency key; unique order/payment attempt; callback signature and amount/currency checks. A customer “I paid” message never marks paid. | Payment command requires tenant/branch/order scope and capability entitlement (LITE unavailable, BASE/PRO permitted only when configured). Provider callback scope is resolved from trusted merchant mapping. Staff manual confirmation/refund needs explicit permission and reason. | Provider transaction/ref, merchant settlement target, amount/currency/fees, callback payload hash/classification, verification result, actor/reason, order reference, refund evidence and reconciliation timestamps. Never log secrets or raw payment credentials. | Gateway timeout is `UNKNOWN` and reconciled, not failed/success. Static QR/UPI/card/cash uses `MANUAL_CONFIRMATION_REQUIRED` until staff evidence. Transient callback/provider failures retry; invalid signature/amount goes quarantine. Replay is provider-state reconciliation, not blind “mark paid”. | UPI/acquirer/gateway adapter; WhatsApp is interaction surface, not payment truth. Direct settlement to merchant; provider selection and legal/KYC remain open. | **P0 pilot gate** for one validated merchant/test provider plus manual confirmation/refund path. Additional gateways and richer payment rails are **P1/later**. | Payment completion must not accept order automatically; amount and merchant match; duplicate callbacks do not duplicate effects; refund/reconciliation evidence is queryable. Measure callback verification failures, pending/unknown age, settlement mismatch, refund latency, manual confirmations and payment incident rate. | **Specified; implementation not started; no executable demo.** | Raw T68 (`bbb21613-1461-4278-a34f-d4c055c03c84`), raw `L61167`; Raw T72 (`bbb21c29-06e2-44d7-bd05-c2f9c28c3f4f`), raw `L63619`; chat payment discussion `nekurama.chatgpt.md:L4212-L4304`; [`architecture-lld.md`](architecture-lld.md), [`domain-model.md`](domain-model.md), [`tier-feasibility-matrix.md`](tier-feasibility-matrix.md). |
| **8. Delivery / pickup** | Actors: customer chooses mode; restaurant prepares; `Fulfillment` owns mode/lifecycle. Pickup: `READY_FOR_PICKUP → COMPLETED`. Delivery: `REQUESTED → QUOTED → CONFIRMED → ASSIGNED → IN_TRANSIT → DELIVERED`, with `FAILED/CANCELLED/RETURNED`. Delivery provider interaction is separate from order and payment. | `ChooseFulfillmentMode`, `GetDeliveryQuote`, `ConfirmAddress`, `ConfirmDelivery`, `BookDelivery`, `CancelDelivery`, `UpdatePickupReady`, `ConfirmCollection`, `ReconcileTracking`; events `FulfillmentSelected`, `QuoteReceived`, `DeliveryBooked`, `DriverAssigned`, `OutForDelivery`, `Delivered`, `PickupCompleted`. `FulfillmentPort` / `DeliveryProviderPort` adapters. | Quote/request id; provider booking/idempotency key; provider event ID + tenant/branch/fulfillment scope; tracking event deduplication. Repeated booking returns existing booking or explicit conflict. | Pickup is available in the pilot; delivery requires BASE/PRO entitlement, provider configuration, valid geography/address and policy. Customer address is fulfillment-context data, not automatically a global customer aggregate. Restaurant may subsidize fee only through an explicit commercial rule. | Mode, branch, address consent/reference, quote components, provider/ref, fee payer, state transitions, status source, timestamps, customer/staff decisions and reconciliation results. | **Pickup-first fallback** is valid when delivery is unavailable; it must be presented as an explicit option, not a silent downgrade after a paid delivery choice. Quote expiry/repricing requires reconfirmation. Transient provider failures retry; booking ambiguity reconciles; poison callbacks DLQ. Replay uses provider status reconciliation. | Provider-neutral adapter for Porter/Rapido/Uber/future provider; no owned fleet. Exact provider/API approval, rates and geography are unresolved. | **P0 pilot gate** for pickup; **P1 gated beta** for delivery only after provider validation and tier entitlement. Fleet/advanced tracking is **later/non-goal**. | Pilot must complete pickup without an address; delivery tests must show quote before payment/confirmation, explicit provider/fee, no order/payment corruption and correct status. Measure pickup completion, quote/book latency, provider error/throttle, tracking divergence, delivery cancellation and fallback rate. | **Specified; pickup architecture pilot-ready; implementation not started; no executable demo.** No delivery provider is approved. | Raw T76 (`bbb21803-3975-4faf-9eda-c51cade91dfe`), raw `L61971`; chat `L2285-L2422` and `L2559-L2626`; [`product-definition.md`](product-definition.md), [`domain-model.md`](domain-model.md), [`tier-feasibility-matrix.md`](tier-feasibility-matrix.md). |
| **9. Promotion / combo** | Actors: owner configures; customer receives evaluated benefit; `Price`, `Promotion`, `Tax` and `Order` own separate contributions. Combo/bundle is a pricing construct; promotion owns conditional benefits. `Order` freezes the result. | `CreatePromotion`, `CreateCombo`, `EvaluateCommercials`, `ApplyBenefit`, `ValidateStacking`, `SnapshotCommercialResult`; events `PromotionCreated`, `ComboPublished`, `CommercialEvaluated`, `BenefitApplied/Rejected`. `PromotionPort` / `ComboPort` are internal policy ports; no external provider is authoritative. | Rule/version ID, calculation-capsule hash, cart/order evaluation key and bounded re-evaluation count. Same cart + same rule/version returns same result; no recursive mutation. | Tenant/branch owner scope; entitlement gate (LITE unavailable, BASE bounded, PRO advanced); customer eligibility and consent are evaluated server-side. AI may recommend but cannot apply a promotion or change price. | Rule/version, eligible facts, excluded reasons, stacking decision, price/tax/provenance, actor, policy/quota version and order snapshot. | Invalid/ambiguous rule is rejected or marked unavailable, never applied silently. Concurrent quota/usage uses atomic reservation; transient reads retry; policy poison goes quarantine. Replay re-evaluates only for a new draft/version; historical order remains frozen. | No provider boundary for core calculation; optional future campaign/channel adapters are downstream notification concerns. | **P1 gated beta** (BASE bounded requests, provisional max 3/day/short activation; PRO only after evidence). Advanced campaigns/stacking are **later**. | Deterministic repeated calculation, no over-discount, tax/rounding trace, explicit unavailable tier outcome, and immutable order result. Measure evaluation latency, rule rejection, quota contention, benefit error, promotion abuse and contribution/cost by tenant. | **Specified; implementation not started; no executable demo.** Semantics for stacking, tax/rounding and abuse remain unresolved. | Tier direction: Raw T35 (`97f25c92-127f-4c8c-bde9-c314010cbef7`), raw `L46376`; chat `L2439-L2673`; [`domain-model.md`](domain-model.md), [`tier-feasibility-matrix.md`](tier-feasibility-matrix.md). |
| **10. Human takeover** | Actors: customer, AI/automation, owner/staff. `Conversation` owns `AUTOMATED → HUMAN_REQUESTED → HUMAN_ACTIVE → HUMAN_RELEASED → AUTOMATED`; support task/assignment records human ownership. Order, payment and fulfillment state machines continue independently. | `RequestHuman`, `AssignConversation`, `SendHumanMessage`, `ReleaseToAutomation`, `PauseAutomation`, `ResumeAutomation`; events `HumanRequested`, `ConversationAssigned`, `AutomationPaused/Resumed`, `HumanMessageSent`. WhatsApp multi-device/coexistence and web inbox adapters are transport surfaces. | Provider message ID, assignment version, takeover lease/token and outbound message idempotency key. Duplicate takeover requests coalesce; automation workers check conversation mode before sending. | Only assigned/authorized owner/staff can act; customer sees restaurant identity. Tenant/branch/channel scope is checked on every message. Takeover does not bypass order/payment/fulfillment authorization or tier gates. | Record trigger, classifier/suggestion, actor, assignment, pause/resume timestamps, messages, redaction/access, release reason and all domain commands caused by human action. | Automation send while human active is blocked/requeued, not sent late out of context. Staff unavailable routes to queue/acknowledgement. Provider outage retries; poison message DLQ. Replay rehydrates conversation projection and assignment history, not business-state side effects. | Meta/WhatsApp coexistence target and web operations surface; exact device/history behavior remains validation. | **P0 pilot gate** and first-class safety path in every tier. Advanced routing/queues are **P1**. | During takeover, no automated message is emitted without an explicit release; an order/payment callback still progresses normally; staff correction is auditable. Measure pause duration, automation-suppression violations, assignment latency, human response time, takeover rate and customer abandonment. | **Specified; implementation not started; no executable demo.** | chat `L522-L557`, `L7450-L7608`; Raw T94 (`53e3d28d-2172-4b99-9d5d-cefc16513fa9`), raw `L21496`; Raw T96 (`43953802-8fdc-4ee4-a329-782796871b42`), raw `L16533`; [`experience-and-channels.md`](experience-and-channels.md), [`domain-model.md`](domain-model.md). |
| **11. Notifications / consent** | Actors: customer, owner/staff, platform admin. `NotificationIntent` and delivery-attempt records own outbound lifecycle; `Consent` owns purpose/channel scope and opt-out. Categories remain separate: service response, transactional, support, marketing/re-engagement. | `CreateNotificationIntent`, `ResolveConsent`, `RenderTemplate`, `SendMessage`, `SendEmail`, `SendSMS`, `RecordDelivery`, `OptOut`, `RetryNotification`; events `NotificationRequested`, `Sent`, `Delivered`, `Failed`, `OptedOut`. Direct service response, approved-template and future Direct Send adapters are selected by policy, not by domain code. | Provider message ID, notification intent ID, template/version and deduplication key `(tenant, subject, category, event, version)`. Replayed order event must not resend an identical notification unless explicitly requested. | Consent is restaurant/tenant-scoped; transactional order status must not depend on marketing opt-in. Staff/admin can send only within role, tenant, purpose and template policy. Customer opt-out is authoritative for the relevant category. | Template ID/version/language, purpose, consent evidence, service-window decision, variables provenance, provider response, retry history, redacted content hash and recipient scope. | Within service window use service response; outside it use approved template or explicit unavailable outcome. Non-retryable template/policy rejection quarantines; transient provider failures retry with `Retry-After`; DLQ needs staff/admin review. Replay is category/consent re-evaluation with version rules, never blind resend. | WhatsApp/Meta primary; email/SMS/push are replaceable adapters. Provider cost and approval are not hidden inside order state. | **P0 pilot gate** for transactional WhatsApp order/payment/pickup updates; marketing/re-engagement is **P1/later** and tier/consent-gated. | Prove no marketing is sent without explicit opt-in, opt-out suppresses future marketing, essential order updates continue, duplicate provider callbacks do not duplicate status, and templates are versioned. Measure send/delivery/failure, template rejection, opt-out, consent conflict, cost per category and queue age. | **Specified; implementation not started; no executable demo.** Provider policy, template approval and India-specific legal wording remain open. | Raw T63 (`2f7e2590-8665-4398-b7d6-334e994d4ab2`), `nekurama.raw.chat.json:L11878`; chat `L7708-L7902`; companion UUID `7c3e0ece-66e5-41cb-8271-41d0c2d09647`, raw `L31945`; [`architecture.md`](architecture.md), [`experience-and-channels.md`](experience-and-channels.md). |
| **12. Retention / reorder** | Actors: customer and restaurant. `TenantCustomer` relationship and consent remain authoritative; `ReorderIntent`/campaign workflow coordinates repeat ordering. Retention is not a platform-wide customer graph and cannot expose one restaurant’s history to another. | `GetEligiblePreviousOrder`, `StartReorder`, `ConfirmRepeatCart`, `RecordRepeatOrder`, `ScheduleWinback`, `CancelCampaign`; events `RepeatOrderSuggested`, `ReorderStarted`, `RepeatOrderCompleted`, `CampaignSuppressed`. Uses order/customer query contracts and notification adapters. | Reorder key includes tenant/customer/source-order; campaign send key includes category/template/version/period. Same source order cannot create duplicate active reorder or duplicate order. | Customer can access only own tenant relationship/history; marketing/re-engagement requires tenant-scoped opt-in and entitlement. Operational repeat ordering from a customer-initiated conversation is distinct from marketing. | Source order/menu revision, customer consent, recommendation reason, excluded unavailable items, actor/channel, campaign/template version and resulting order linkage. | If source item unavailable, offer explicit replacement/new cart; if consent missing, suppress marketing but allow customer-initiated reorder. Provider failures retry/DLQ; stale history is refreshed. Replay rebuilds eligibility, never sends historical campaigns. | Notification/channel adapters only; no external “retention platform” is selected. | **P1 controlled beta** for customer-initiated reorder; outbound win-back/campaigns are **later/PRO-gated**. | Demonstrate a customer can repeat from their own tenant history without marketing consent, and another tenant cannot see it. Measure repeat-order rate, reorder conversion, suppression/opt-out, stale-menu corrections and incremental message/provider cost. | **Specified; implementation not started; no executable demo.** Product value is a validation hypothesis, not a retention guarantee. | chat `L587-L611`, `L7800-L7902`; Raw T71 (`4bbdb489-0a0d-45d5-af27-70535c5d4acc`), raw `L20443`; Raw T290 (`4702681b-d611-4408-af5f-9001d04b6cfa`), `nekurama.raw.chat.json:L18554`; [`validation.md`](validation.md), [`domain-model.md`](domain-model.md). |
| **13. Exceptions / recovery / reconciliation** | Actors: workflow/reconciliation operator, owner/staff, platform admin, provider support. Domain modules own `FAILED`, `UNKNOWN`, `PENDING_REVIEW`, `CANCELLED` states; `Workflow`, `Reconciliation`, `Audit` own orchestration/history, not business truth. | `RetryCommand`, `Quarantine`, `ResolveDLQ`, `ReconcileProvider`, `Compensate`, `RestoreProjection`, `AuthorizeReplay`; events `RetryScheduled`, `Quarantined`, `Reconciled`, `CompensationApplied`, `ReplayAuthorized`, `ReplayStarted/Completed`. Managed queue/outbox/inbox and provider adapters are infrastructure ports. | Inbox key, outbox event ID, provider event ID, command idempotency key, aggregate version and replay run ID. Replay is bounded by schema/version and explicit authorization. | Replay/DLQ actions require least-privilege operational scope, tenant/branch/flow restriction and reason. No operator may replay an external side effect blindly or widen tenant scope. | Immutable attempt history, error classification, retry schedule, DLQ reason, operator approval, provider response/checksum, reconciliation result and before/after state. | Transient failures retry at most five times with jittered `1s, 5s, 30s, 5m, 30m`; validation/auth/policy failures quarantine; human-action failures create tasks. Reconcile ambiguous payment/delivery before compensation. Restore uses last known-good image/database and replay procedure. | Meta, payment and delivery adapters expose status/reconciliation APIs; exact queue/workflow implementation is unresolved. | **P0 pilot gate** for outbox/inbox, retry, DLQ, reconciliation and restore drill. Advanced workflow product/extraction is **later after POC evidence**. | Inject provider timeout, duplicate callback, poison payload and DB/worker restart; prove no false success, bounded retry, visible DLQ, safe replay and RPO 24h/RTO 8h restore evidence. Measure oldest outbox, queue age, retry count, DLQ/quarantine, reconciliation mismatch, replay duration, backup age and restore duration. | **Specified; implementation not started; no executable demo.** Workflow engine, broker, replay store and exact SLOs remain unresolved. | Raw T115 (`bbb210cc-a1ef-4ea9-b1a7-7c52f0011721`), raw `L58590`; Raw T119 (`bbb211c0-c142-4837-aca7-b67a21454ec8`), raw `L58920`; Raw T125 (`bbb21b98-7460-45d5-a616-418ffbf47484`), raw `L63531`; Raw T126 (`e472fe3d-22d5-4595-aafd-986683b13a3c`), raw `L75611`; Raw T264 (`bbb21a7f-4d44-4cc1-8f60-1172bdcc303c`), raw `L63179`; [`architecture-lld.md`](architecture-lld.md), [`architecture-boundaries.md`](architecture-boundaries.md). |
| **14. Admin / analytics** | Actors: platform admin, owner, manager, staff with reporting scope. Domain modules remain authoritative; analytics/read models are eventually consistent. `AdminCase`/operational views own workflow queues; no dashboard becomes source of truth. | `ListOnboardingFunnel`, `ListOrders`, `GetOperationalMetrics`, `GetCostLedger`, `ExportTenantData`, `ReviewDLQ`, `ReviewAudit`, `RecordPilotMetric`; events consumed from outbox/event stream into projections. Web dashboard is dense surface; WhatsApp supports bounded admin actions. | Projection consumer inbox/event ID; report query snapshot/version; export job ID/checksum. Re-running a projection is safe and does not mutate domain state. | Platform admin scope is separate from tenant owner scope; owner sees only tenant/branch data and approved customer relationship fields; staff sees operational subset. Export and audit access require explicit policy and are logged. | Metric definitions, source event IDs, projection version, query filters, tenant/branch, export checksum, requester, purpose, retention/classification. Never place unrestricted PII or secrets in analytics logs. | Projection lag/rebuild uses replay from retained events; malformed events quarantine without blocking authoritative state. Query timeout returns explicit retry/async-export outcome. Deletion/retention policy must remove or redact projections consistently. | Web/BFF and optional WhatsApp admin adapter; telemetry backend, object storage and analytics store remain replaceable. | **P0 pilot gate** for operational order/onboarding/error views and cost/support ledger. Advanced BI, warehouse and cross-tenant benchmarking are **later/non-goal**. | Dashboard must reconcile to authoritative order/payment/fulfillment counts within a stated lag, show stale/error state, and prevent cross-tenant reads. Measure projection lag, rebuild duration, query latency/error, export failures, audit-read volume and metric completeness. | **Specified; implementation not started; no executable demo.** Static preview is not an analytics implementation or pilot evidence. | chat `L1774-L1845`, `L1576-L1638`, `L3181-L3213`; Raw T100 (`39e2f9bb-f490-461b-a7c0-ddc8baf13701`), raw `L14361`; [`architecture-lld.md`](architecture-lld.md), [`validation.md`](validation.md), [`experience-and-channels.md`](experience-and-channels.md). |

## Non-negotiable cross-family invariants

1. **Payment is not order.** Payment completion, payment failure, manual
   confirmation, refund and reconciliation update `Payment`; only the Order
   aggregate can accept/reject or progress an order. A payment callback cannot
   accept an order, and an order callback cannot mark a payment paid.
2. **Human takeover is conversation-scoped.** Automation is paused or
   suppressed for the conversation while human ownership is active. Order,
   payment, fulfillment, timers, outbox/inbox processing and reconciliation
   continue independently. Releasing a conversation does not replay stale
   automated messages without a fresh context check.
3. **Pickup is the first fulfillment.** Pickup requires no customer address and
   is the fallback only when the customer explicitly accepts it. Delivery is
   an optional provider-backed capability, tier-gated and unavailable until
   quote/booking/status behavior is validated.
4. **Promotions and combos are tier-gated.** LITE has no promotion/combo
   workflow; BASE has bounded deterministic controls; PRO has higher/advanced
   controls only after policy, quota, abuse, tax/rounding and provider/channel
   evidence. An unavailable tier is an explicit non-success outcome.
5. **Direct merchant settlement.** Customer money goes to the merchant via a
   validated UPI/gateway/manual path. BABAI does not introduce a wallet,
   escrow or customer-funds custody layer in the pilot.
6. **AI is advisory.** Extraction, intent, recommendations and drafts become
   typed commands and pass authorization, policy, deterministic validation and
   human approval where required. AI cannot choose tenant, branch, entitlement,
   order, payment, refund, fulfillment, consent or external side effects.
7. **Historical truth is frozen.** An order stores its commercial result,
   applied benefits, tax/rounding inputs and menu revision. Later menu,
   promotion, price or provider changes do not rewrite the order.
8. **Events are not state.** Events, workflow records, audit and telemetry
   coordinate or describe the system. Aggregate state and validated
   transitions remain authoritative.

## Acceptance and observability gates

The matrix is accepted for design handoff only when the implementation plan
preserves these gates:

- **Boundary:** wrong tenant, branch, role, entitlement or consent is rejected
  explicitly and appears in audit/metrics.
- **State:** every accepted transition has a version/precondition, an atomic
  authoritative write, outbox record and required audit record.
- **Reliability:** duplicate inbound/provider events, worker restarts and
  retries do not create duplicate orders, payments, bookings or messages.
- **Failure:** timeout/unknown, non-retryable validation, human-action and
  provider reconciliation cases remain visibly distinct; no success-shaped
  fallback is permitted.
- **Human safety:** active takeover suppresses automation while independent
  order/payment/fulfillment processing remains correct.
- **Commercial integrity:** order snapshot is reproducible; payment amount,
  merchant and currency are verified; direct settlement is observable.
- **Tier integrity:** LITE/BASE/PRO checks happen server-side at gateway,
  worker and owning module; quotas are atomically consumed or explicitly
  denied.
- **Continuity:** pilot evidence includes daily backup success, tested restore,
  initial RPO 24 hours and RTO 8 hours.
- **Pilot instrumentation:** activation, order conversion/completion, pickup
  completion, staff adoption/takeover, support effort, provider/API cost,
  failure/refund cost, repeat use and tier adherence are captured per
  tenant/branch.

Minimum alert set: failed backup, oldest outbox/queue age, DLQ/quarantine
growth, callback verification failures, ambiguous payment settlement,
delivery/provider failure spikes, projection lag, suspected scope violation,
automation-suppression violation, and founder-support capacity pressure.

## Current unresolved decisions

- Exact State Engine schema and whether workflow execution is custom,
  open-source or managed; Temporal is not selected.
- PostgreSQL schema/module layout, projection/event-history retention and
  deletion behavior.
- Meta Tech Provider approval, existing-number coexistence, account topology,
  templates/Direct Send, disconnect and provider-exit behavior.
- Payment provider/acquirer, webhook/refund evidence, manual-confirmation
  controls and legal/accounting responsibilities.
- Delivery provider/API access, geography, quote/booking cancellation and
  customer/merchant fee treatment.
- Promotion stacking, combo composition, tax/rounding and abuse limits.
- Consent wording, retention/deletion periods, subprocessors, data location,
  encryption/key management and incident operations.
- Production SLOs beyond the continuity baseline and the exact pilot numeric
  thresholds; `validation.md` keeps proposed thresholds separate from
  confirmed decisions.

The durable current architecture remains [`architecture.md`](architecture.md),
[`architecture-boundaries.md`](architecture-boundaries.md),
[`architecture-lld.md`](architecture-lld.md) and
[`domain-model.md`](domain-model.md). This matrix is their flow-oriented
implementation and demo-status companion, not a provider or legal approval.
